class Post:

    def __init__(self, name, price, condition, link, subscription, has_delivery, page):
        self.name = name
        self.price = price
        self.condition = condition
        self.link = link
        self.subscription = subscription
        self.has_delivery = has_delivery
        self.page = page
